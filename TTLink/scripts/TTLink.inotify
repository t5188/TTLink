#!/system/bin/sh
cd ${0%/*} # current working directory
events=$1
monitor_file=$3

if [[ "${monitor_file}" == "disable" ]]; then
  case "${events}" in
  "d")
    ./TTLink.service enable >/dev/null 2>&1
    ;;
  "n")
    ./TTLink.service disable >/dev/null 2>&1
    ;;
  esac
fi

# TTLink.inotify
